{
  "name": "stackads-celo",
  "version": "1.0.0",
  "description": "StackAds ERC20 token contract on Celo",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network celoAlfajores",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network celo",
    "verify:testnet": "hardhat verify --network celoAlfajores",
    "verify:mainnet": "hardhat verify --network celo"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^20.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0",
    "dotenv": "^16.4.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
